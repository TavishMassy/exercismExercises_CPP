#pragma once
#include <string>
#include <vector>
#include <stdexcept>

namespace series {

// TODO: add your solution here
std::vector<std::string> slice(std::string sequence, size_t length);

}  // namespace series
